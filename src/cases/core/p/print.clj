(ns cases.core.p.print)

;; Prints the object(s) to the output stream that is the current value
;; of *out*.  print and println produce output for human consumption.
(print "foo")
(print "foo")
(print "foo")
(newline)