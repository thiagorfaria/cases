(ns cases.core.vars.*ns*)

;; A clojure.lang.Namespace object representing the current namespace.
(prn *ns*)

(ns cases.core.vars.+)

(prn *ns*)
(prn (str *ns*))

(ns cases.core.vars.*ns*)
(prn (str *ns*))
